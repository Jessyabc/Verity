# Runs monitor:once — pilot URLs from pilot-urls.json PLUS every company_sources row in Supabase
# (SEC / admin-added) that is not the same slug+source_key as a pilot JSON entry.
# Secrets: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY

name: Monitor once

on:
  workflow_dispatch:
  schedule:
    - cron: '25 */6 * * *'

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install
        run: npm ci

      - name: Run monitor
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npm run monitor:once
